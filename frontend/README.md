# Inventory Studio (Frontend)

## Setup

```bash
pnpm install
pnpm dev
```

## Environment

Copy `.env.example` to `.env` and adjust the API URL if needed.

```
VITE_API_URL=http://localhost:5000/api
```
